// Створення об'єкта класу Row з ініціалізацією списком символів {'1', '3', '5', '2', '4'}
Row row = new Row(new List<char>() { '1', '3', '5', '2', '4' });

// Виводиться довжина списку (кількість символів)
Console.WriteLine(row.GetLength());

// Виводиться список символів у вигляді масиву (але відобразиться тип System.Char[], потрібно перетворити на рядок для коректного виводу)
Console.WriteLine(new string(row.GetValues()));

// Видаляється перше входження символу '5' зі списку
row.RemoveFive();

// Виводиться оновлений список символів (вже без '5')
Console.WriteLine(new string(row.GetValues()));
